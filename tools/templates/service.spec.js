import { <%= className %> } from './<%= name %>.service';

describe('<%= className %>', function() {
    let instance;

    beforeEach(function () {
        instance = new <%= className %>();
    });


});